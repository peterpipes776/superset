export function ServicesSection(_props: {
	data: unknown;
	isLoading: boolean;
	onRestart: (cmd: string) => void;
}) {
	return (
		<div className="p-4 text-sm text-muted-foreground">
			Services — TODO: reconstruct
		</div>
	);
}
